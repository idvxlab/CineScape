可交互的创作意图对齐系统
两阶段创意系统——前半段把用户模糊的自然语言意图，通过对话和选择控件对齐到一个预先存在的结构化「设计空间」；后半段在对齐后的意图约束下、结合从设计空间召回的参考，生成可编辑的镜头脚本。
ideas
1. 第一阶段的意图对齐看似是消除分歧，实际上选项本身就是一次创意行为。LLM 对用户模糊意图生成若干解读，故生成选项的 agent 既要锚定设计空间，又要保证几个选项导向不同镜头语言。
2. 十个镜头参数并非各自独立，agent 有可能会产出自相矛盾的镜头脚本。比如景别和焦段有可能相互约束，长焦(焦段)会压缩空间，而广角(景别)会强调纵深。
3. 一个镜头脚本是一个序列，提出的不同镜头脚本方案 A/B/C 应该是三种不同的电影化策略，而不是同一套镜头的参数微调。
多智能体架构

```
                    用户：模糊意图
                          │
                          ▼
   ┌──────────────────────────────────────┐
   │ 阶段一 · 意图对齐                        │◄──► 设计空间·意图本体（维度 + 受控词表）
   │   意图对齐 Agent（推理式解释 + 控件）     │
   │   ↺ 多轮：选项/滑块 ⇄ 用户选择            │
   └──────────────────────────────────────┘
                          │ 对齐产物：brief（一段）+ tags
                          ▼
       召回（tags 过滤 + brief 语义检索）◄──► 设计空间·方案库（意图 → 镜头脚本范例）
                          │ 一组多样的参考范例
                          ▼
   ┌──────────────────────────────────────┐
   │ 阶段二 · 镜头脚本生成                     │
   │  ① 策略归纳：从范例归纳 3 个真正不同的    │
   │     电影化方向（数据驱动，非写死）         │
   │  ② 每个方向并行 plan→detail：             │
   │     骨架(beats) → 十参数镜头 + 推荐理由    │
   │  ③ 一致性审校 Critic（参数耦合 + 意图忠实）│
   └──────────────────────────────────────┘
                          │ A / B / C 方案
                          ▼
           用户选择 + 编辑 ⇄ 编辑协作 Agent
                          │
                          └── 采纳/编辑结果 + 对齐意图 回写 ──► 设计空间·方案库（飞轮）

   导演 / Orchestrator 贯穿全程：状态机 · 路由 · 对齐收敛（推理判定）· OOD 兜底
```
1. 意图对齐 Agent
输入：当前 Intent State（目前推测的用户意图）、用户最新输入（初始意图 / 一次选择 / 一段自由补充）、本体 schema（设计空间各维度）。
输出：更新后的 Intent State  +  一组控件（如果需要进一步询问用户拆解意图）/复述对齐后的完整意图供用户确认（如果系统已认为信息量已经足够）。
可以被逐轮调用，直到被 orchestrator 认定收敛。
设计哲学：直接让 agent 通读已知信息，推理判断：对「这一个具体意图」而言，哪些维度既对最终镜头语言影响重大、又仍不明确——优先就这些发问。
可选的控件类型（直接供 Agent 选择调用）：
- 单选：互斥的若干选项
- 多选：可并存（共情引导 同情 + 共情可同时；情绪唤起 多情绪并存）
- 滑块/程度：连续维度（距离感知 亲密 ←→ 公共；节奏特性 舒缓 ←→ 急促；情绪浓度 克制 ←→ 浓烈）
- 自由文本 + 建议词：永远保留的逃生口（也是创作者潜台词 → 叶子的入口）
- 确认/否定：针对那句复述
若在多轮对话中检测到矛盾（既"主动选择的独处"又透出"渴望被理解"），不要默默挑一个，而是把矛盾点做成控件抛回用户。
Prompt 设计 demo：
你是创作意图对齐者。把用户模糊的表达映射到「意图本体」，用最少的提问帮其澄清。

如何推理决定问什么：
- 通读已知信息，判断：对「这一个具体意图」而言，
  哪些维度既对最终镜头语言影响重大、又仍不明确——优先就这些发问。
- 尊重依赖：前置维度未定则先不问其后置维度。
- 每轮最多 3 个控件。

维度状态用定性标签：open / leaning / resolved / conflicting / blocked_by:X，
由你推理得出，不要打数值分。

[本体 schema] [当前 Intent State] [用户最新输入]

输出 JSON：reflection（一句可被否定的复述）、dimension_updates、
widgets（按维度性质选 single/multi/slider/free-text）、ready_to_generate。
由 orchestrator 进行收敛判断：
- ① 要紧维度集（黏滞）。 orchestrator 显式维护"对这个意图要紧的维度"集合（即 §5.2 里"影响重大"的那些），每个附一句理由。它黏滞：只在新证据下增删，不每轮从头重推——这是抑制判定抖动的关键。判要紧用反事实："该维度翻转取值，会不会改变方向级镜头策略？" 不会 → 不要紧 → 不该再问（防过晚）。
- ② 收敛谓词（确定性）。 要紧集定下后，收敛可机械检查：每个要紧维度 ∈ {resolved, 可接受 leaning}，且无 conflicting，且无未决 blocked_by。把唯一的模糊性收进"哪些要紧""leaning 算不算可接受"两点，其余交确定性谓词——稳定性即来于此。
你在判断"对齐能否收敛、进入生成"。

1. 维护"设计空间维度集"：哪些维度对【这个意图】要紧？各给一句理由。
2. 逐个判要紧维度：resolved / 可接受 leaning / conflicting / blocked_by。
   resolved 须能说出下游镜头后果；说不出 → 要么不要紧、要么没真定。
3. 判断该维度是否已经对齐。
4. 放行前输出可被否定的复述，待用户确认。

[本体 schema] [当前 Intent State（含 key_dimensions）] [对话历史]
输出 JSON：key_dimensions(含理由)、per_dim 判定、converge、defer_to_stage2[]、reflection。
2. 设计空间召回
对齐后的 Intent State 在设计空间中进行混合检索：
- tags 做结构化过滤
- brief 做语义相似度
目标是返回一组本身就有策略差异的范例。
3. 镜头脚本生成 Agent
输入：对齐 Intent State（含 brief）+ 召回范例组 + 电影语法基础知识（可选）。
输出：A/B/C 镜头脚本，每个含 strategy + 镜头序列（十参数 + 每镜推荐理由）+ overall_rationale。
第一步 · 策略归纳。分析召回范例组，识别其中真正不同的电影化路数（长镜头留白 / 碎片蒙太奇 / 缓慢推近渐强…），归纳出至多 3 个对当前意图真正分叉的方向；数量取决于范例实际支持几种，不硬凑。每个方向说明它锚定哪些范例、核心手法是什么。这些方向就是这一轮的"导演声音"——从设计空间 induce 出来。
第二步 · 每个方向 plan->detail。plan：先出镜头序列骨架（每个 beat：意图功能 + 大致景别/运镜），体现该方向的结构 DNA。不同策略的最大分叉体现在结构层，所以先定骨架。detail：把每个 beat 填成完整十参数镜头 + 推荐理由（链接到它服务的对齐的意图维度）。
Prompt 设计：
你是镜头设计师。你的电影化策略与具体手法，应从下面这组「设计空间召回范例」
中归纳与汲取，而不是套用任何预设的导演风格。

第一步 · 策略归纳：
- 分析这组范例用了哪些不同的电影化路数。
- 归纳出至多 3 个对当前意图真正不同的方向；数量取决于范例实际支持几种，不硬凑。
  每个方向说明它对应哪些范例、核心手法是什么。

第二步 · 每个方向 plan→detail：
- 先出镜头序列骨架（每 beat：意图功能 + 大致景别/运镜），体现该方向结构。
- 再把每个 beat 填成完整十参数镜头 + 推荐理由（链接到它服务的意图维度）。
- 参数从该方向范例汲取，但为「这一个精确意图」改写，不要复制。

铁律：意图忠实 > 范例原貌。范例与意图情绪冲突时，保留结构手法、让调色服从意图。

[Intent State（含 brief）] [召回范例组] [电影语法 KB（兜底/术语）]

输出：每个方向一个 { scheme_id, strategy, shots[…十参数+rationale], overall_rationale }
4. 一致性审校 Agent
- 参数逻辑正确：一个方案内的若干镜头连贯，色调/光线和谐等。
- 意图忠实：分析镜头参数和对应的解释，分析该脚本是否真的服务设计意图。
5. 编辑协作 Agent
用户选定后编辑时介入：校验编辑、在改动破坏一致性时提示（"你把景别改成特写，却还留着大量留白，这俩冲突"）、可只重生成某一个镜头而保持其余不动。
本质是生成 agent 的另一种模式。
6. [可选] 反馈沉淀
用户最终采纳/编辑后的脚本，连同它对齐到的 Intent State（brief + tags），作为新记录回写设计空间方案库。
预期效果：召回质量随使用提升、策略分叉越来越丰富、OOD 空白被逐步填上。这是让系统从"静态库"变成"学习系统"的关键，也让整套系统越来越少依赖任何外部强加的审美——一切收敛回设计空间自身。