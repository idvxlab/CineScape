# 创意意图对齐系统 · 架构设计（地基）

技术栈定稿：**Python 后端（FastAPI + LangGraph + Postgres/pgvector）+ React 前端**，按"可演进到生产"建。
本文件钉死四件事：编排图、核心契约 schema、API/事件契约、目录与落地顺序。

---

## 1. 为什么是这套栈（一句话回顾）

| 层 | 选型 | 命中的硬约束 |
|---|---|---|
| Agent 编排 | **LangGraph** | orchestrator = 带共享状态的状态机；对齐/选择两处*中断等用户*靠 `interrupt`+checkpointer；并行 fan-out + 生成⇄Critic 循环 |
| 后端 API | **FastAPI** | async + SSE 流式；Pydantic 模型即 IntentState/Widget/ShotScript 的 schema 源 |
| 存储（三合一） | **Postgres + pgvector** | 一个库：LangGraph checkpoint（会话状态）+ 向量召回 + tags 关系过滤 |
| 前端 | **React + Vite** | widget registry 渲染服务端驱动 UI；Zustand 管脚本编辑态 |
| 类型同步 | Pydantic → OpenAPI → `openapi-typescript` | widget 协议两端不漂移 |

---

## 2. LangGraph 编排图

orchestrator 不是一个"agent"，而是**这张图本身**：节点 = agent/工具，边 = 路由，两处 `interrupt` = 人在环。

```
                       START
                         │
                         ▼
            ┌───────► align_agent ◄──────────┐  (多轮)
            │            │                    │
            │            ▼                    │
            │     convergence_check           │
            │       │         │               │
   未收敛: interrupt│         │ 已收敛(合成 brief+tags, confirm)
   抛 widgets 等用户─┘         ▼
                          strategy  (设计空间直推 ≤3 机制路径;
                              │      库内命中仅作可选增强, ADR-0010)
                              │  Send() 并行 fan-out
                ┌─────────────┼─────────────┐
                ▼             ▼             ▼
        generate_dir A  generate_dir B  generate_dir C   (plan→detail)
                └─────────────┼─────────────┘
                              ▼
                           critic ──fail──► (重生成对应方向)
                              │ pass
                              ▼
                   present_candidates  ── interrupt 抛 A/B/C 等用户选择
                              │
                   ┌──────────┴──────────┐
              选择+编辑                 采纳
                   ▼                     ▼
              edit_collab ──► (回 present)  writeback(可选) ──► END
```

**节点清单**

| 节点 | 类型 | 职责 | 出边 |
|---|---|---|---|
| `align_agent` | LLM | 读 state + 用户上轮回应 → 更新 dimensions、出 widgets 或复述；易混淆区用判别 probe | → convergence_check |
| `convergence_check` | LLM(判定) | 维护要紧维度集（黏滞）+ 定性收敛判定 + confirm 门控 | 未收敛→`interrupt`；收敛→strategy |
| `strategy` | LLM | 设计空间直推：对意图组合枚举 ≤3 个机制路径（知识卡供机制与手法）；尽力查库作增强 | `Send` 并行派发 generate_dir |
| `generate_direction` | LLM | 单方向 plan→detail（并行多实例），三层链：效果→机制→手段→十参数，每镜带 serves | → critic |
| `critic` | LLM+规则 | 耦合规则表 + serves 确定性检查 + 机制链忠实/易混淆误用/双极轴自洽 | fail→重生成；pass→present |
| `present_candidates` | `interrupt` | 抛 A/B/C 等用户 | 选择→edit_collab；采纳→writeback |
| `edit_collab` | LLM | 校验编辑、单镜重生成、revalidate | → present_candidates |
| `writeback` | 工具 | 飞轮回写方案库（供将来检索增强） | → END |

**关键 LangGraph 用法**
- 状态机：`StateGraph(SessionState)` + `add_conditional_edges`（convergence/recall-signal/critic 的分支路由）。
- 人在环：节点内 `interrupt(payload)` 暂停并把 payload（widgets / candidates）交给调用方；前端回应后用 `Command(resume=user_response)` 续跑。**这是对齐多轮与选择编辑的机制核心。**
- 并行：`strategy_induction` 返回多个 `Send("generate_direction", {dir})`，map 式并行；`candidates` 用带 reducer 的字段收集。
- 持久化：`graph.compile(checkpointer=PostgresSaver(...))`，`thread_id = session_id` → 每个会话是可恢复线程，Intent State 自动跨轮持久化。

---

## 3. 核心契约 Schema（前后端的真正接口）

Pydantic 定义在后端，经 OpenAPI 生成 TS 类型给前端。下面是骨架（非全量字段）。

### 3.1 Widget 协议（服务端驱动 UI 的命门）

```python
# backend/app/schemas/widget.py
from typing import Literal, Annotated, Union
from pydantic import BaseModel, Field

class Opt(BaseModel):
    value: str
    label: str
    hint: str | None = None

class SingleWidget(BaseModel):
    kind: Literal["single"] = "single"
    dim: str                      # 本体维度 id，如 "认知/视角控制"
    prompt: str
    options: list[Opt]

class MultiWidget(BaseModel):
    kind: Literal["multi"] = "multi"
    dim: str
    prompt: str
    options: list[Opt]

class SliderWidget(BaseModel):
    kind: Literal["slider"] = "slider"
    dim: str
    prompt: str
    ends: tuple[str, str]         # ("亲密", "公共")
    ticks: list[str] | None = None

class FreeTextWidget(BaseModel):
    kind: Literal["freetext"] = "freetext"
    dim: str | None = None
    prompt: str
    suggestions: list[str] = []

class ConfirmWidget(BaseModel):
    kind: Literal["confirm"] = "confirm"
    reflection: str               # 可被否定的复述

Widget = Annotated[
    Union[SingleWidget, MultiWidget, SliderWidget, FreeTextWidget, ConfirmWidget],
    Field(discriminator="kind"),   # → TS 侧也是判别联合，前端 registry 直接 switch(kind)
]
```

### 3.2 Intent State

```python
# backend/app/schemas/intent.py
from typing import Literal
from pydantic import BaseModel

DimStatus = Literal["open", "leaning", "resolved", "conflicting", "blocked_by"]

class DimensionState(BaseModel):
    value: str | None = None
    candidates: list[str] = []
    status: DimStatus
    blocked_by: str | None = None        # status=blocked_by 时填上游维度 id

class IntentState(BaseModel):
    raw_intent: str
    dimensions: dict[str, DimensionState] = {}   # key = 本体维度 id（跨层多标签）
    key_dimensions: list[str] = []               # 黏滞要紧集，收敛谓词只看它们
    brief: str | None = None                      # 收敛时合成
    tags: list[str] = []                          # 收敛产出 = 选中的本体叶子
```

### 3.3 Shot Script（十参数）

```python
# backend/app/schemas/shotscript.py
from pydantic import BaseModel

class Shot(BaseModel):
    order: int
    shot_size: str        # 景别
    composition: str      # 构图
    angle: str            # 角度
    movement: str         # 运镜
    focal_length: str     # 焦距
    depth_of_field: str   # 景深
    lighting: str         # 光影
    color_tone: str       # 色彩
    rhythm: str           # 节奏
    duration: str         # 时长
    serves: list[str]     # 本镜服务的二级意图 code（critic 据此查机制链）
    rationale: str        # 机制链解释：参数为何服务这些意图
    frame_edit_hint: str  # 基底图→本镜画面的图像编辑指令（有基底图时产出, ADR-0011）
    frame_image: str | None  # 渲染帧 URL（生图 API 接入后填充）

class ShotScript(BaseModel):
    scheme_id: str                  # "A" / "B" / "C"
    strategy: str                   # 方向描述
    dominant_intents: list[str]     # 该方向主导的二级意图 code（机制锚定, ADR-0010）
    mechanism: str                  # 该方向依赖的心理/知觉机制
    shots: list[Shot]
    overall_rationale: str
```

### 3.4 方案库记录（Postgres + pgvector）

```python
class RecallRecord(BaseModel):
    record_id: str
    intent_tags: list[str]          # 本体叶子，结构化过滤用
    intent_brief: str
    # embedding: vector(768)        # pgvector 列，brief 语义检索用
    shot_script: ShotScript
    provenance: Literal["curated", "user_accepted"]
    quality_signal: float | None = None
```

---

## 4. API / 事件契约

会话是有状态线程;交互 = 推进到下一个 `interrupt`。**用户决定一律通过 `Command(resume=...)` 传入、
由门控节点捕获**;API 层不直接改图状态。TurnResponse 由"挂起的 interrupt payload"派生
(无挂起 interrupt 时按 state.phase 兜底:strategy/generate→generating,writeback/done→done)。

| 方法 | 路径 | 作用 |
|---|---|---|
| `POST` | `/sessions` | JSON `{raw_intent}` 或 multipart `raw_intent`+`image`(参考基底图,ADR-0011)→ 建会话,跑到首个 interrupt,返回 `TurnResponse`(含 `session_id`/`reference_image`) |
| `GET` | `/api/uploads/{file}` | 基底图与(将来)镜头渲染帧的静态服务 |
| `POST` | `/sessions/{id}/respond` | `{dim_widget_responses, free_text?}` → resume ask_user 门控 |
| `POST` | `/sessions/{id}/confirm` | `{confirmed, rejection_text?}` → resume confirm_gate(确认→生成;否决→回对齐) |
| `POST` | `/sessions/{id}/select` | `{scheme_id, action: "writeback"\|"edit"}` → resume present 门控 |
| `POST` | `/sessions/{id}/edit` | `{patch: [{shot_order, field, value}], free_text?}` → resume edit 门控,应用+revalidate |
| `POST` | `/sessions/{id}/render` | `{scheme_id}` → 逐镜关键帧渲染(基底图+重摄指令,ADR-0012);工具调用,不推进图 |
| `GET` | `/sessions/{id}` | 拉当前 state + 可恢复的 turn(断点续玩) |

错误语义:会话不存在 → 404;当前没有挂起的 interrupt、或挂起类型与端点不匹配 → 409。

**TurnResponse(前端按 phase 切 UI,每个分支都带 session_id)**

```ts
type TurnResponse =
  | { phase: "align"; reflection: string; widgets: Widget[]; converged: false }
  | { phase: "confirm"; reflection: string; brief: string; tags: string[]; converged: true }
  | { phase: "generating"; progress: { dir: string; done: boolean }[] }
  | { phase: "candidates"; schemes: ShotScript[]; conflicts: Conflict[];
      selected_scheme_id: string | null }                 // conflicts 来自上一轮编辑的 revalidate
  | { phase: "edit"; scheme: ShotScript; conflicts: Conflict[] }   // 等待 patch
  | { phase: "done"; scheme?: ShotScript };
```

---

## 5. 持久化：一个 Postgres 三用

- **会话状态**：LangGraph `PostgresSaver` checkpoint 表，按 `thread_id` 存全量 SessionState → 断线可恢复、可审计每轮。
- **方案库（检索增强,可选）**：`solution_library` 表，`intent_tags`（`text[]`，GIN 索引做过滤）+ `embedding`（`vector`，HNSW 索引做语义）→ 混合查询一条 SQL：
  ```sql
  SELECT * FROM solution_library
  WHERE intent_tags && :query_tags                 -- 结构化过滤
  ORDER BY embedding <=> :brief_embedding LIMIT 12; -- 语义相似
  ```
  ADR-0010 后该表不在主路径上：strategy 节点尽力查询，命中作附加参考，失败/为空不阻塞。writeback 飞轮继续回写，库长起来后增强自然生效。
- **本体 v3**：`labels_v3.json`（分类内容,自仓库根同步）+ `meta_v3.yaml`（A/B 类型/双极轴/值集/作用域/易混淆规则）+ `knowledge_v3.yaml`（56 张电影语法知识卡）→ `backend/app/ontology/loader.py` 启动合并校验为只读对象，以 digest 形式喂 align/convergence/strategy/generate/critic。规格见 [`docs/domain/ontology-spec.md`](docs/domain/ontology-spec.md)。

---

## 6. 目录结构（monorepo）

```
cinedesign/
├── backend/
│   ├── pyproject.toml
│   └── app/
│       ├── main.py              # FastAPI + 路由挂载
│       ├── api/                 # sessions / stream / edit 处理器
│       ├── graph/
│       │   ├── state.py         # SessionState (Pydantic)
│       │   ├── build.py         # StateGraph 装配 + checkpointer
│       │   └── nodes/           # align, convergence, strategy,
│       │                        #   generate, critic, edit, writeback
│       ├── schemas/             # widget.py intent.py shotscript.py recall.py
│       ├── ontology/            # labels_v3.json + meta_v3.yaml + knowledge_v3.yaml + loader
│       ├── recall/              # pgvector 检索基础设施（可选增强 + 飞轮回写）
│       ├── llm/                 # 模型客户端封装 + prompt 模板
│       └── db/                  # 连接、迁移、PostgresSaver 接线
├── frontend/
│   ├── package.json
│   └── src/
│       ├── types/api.ts         # ← openapi-typescript 自动生成
│       ├── api/                 # 类型化 client + SSE 订阅
│       ├── widgets/             # registry: Single/Multi/Slider/FreeText/Confirm
│       ├── components/          # AlignmentPanel, ScriptCompare, ShotEditor
│       └── store/               # Zustand session store
├── labels_v3.json               # 本体内容权威源（12 一级 × 56 二级）
└── 导演意图分类_v3_中文.xlsx     # 人工编辑入口（改后导出 labels_v3.json）
```

前端核心是 `widgets/registry.ts`：`{ single: SingleWidget, multi: ..., slider: ... }`，按 `widget.kind` 渲染——后端加控件只动后端。

---

## 7. 落地顺序（front-load 风险）

| **M0 地基** | 仓库骨架 + 核心 schema + Postgres + 本体加载 | 契约先行，前后端可并行 |
| **M1 对齐闭环** | align + convergence + `interrupt` + widget 协议 + 最小 React 渲染器 | **最 novel、最高风险的 HITL 交互，先打通** |
| **M2 本体 v3 + 纯推理生成** | v3 三源合并加载 + 知识卡 + strategy 直推 + 并行 plan→detail + critic 三层 | 价值主轴（ADR-0010，取代原"M2 召回 + M3 生成"） |
| **M3 选择/编辑** | present + edit_collab + revalidate | 闭合用户侧 |
| **M4 飞轮 + 检索增强** | writeback 成熟化；库有量后 strategy 检索增强自然生效 | 长期增益 |

冷启动问题已由知识卡解决（ADR-0010）；评估体系仍是开放项，不阻塞当前里程碑。
